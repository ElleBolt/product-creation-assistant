jQuery(document).ready(function ($) {
    // Handle dynamic attribute addition
    $(document).on('click', '.add-attribute', function () {
        // Logic to add new attributes
    });

    // Handle dynamic variation generation
    $(document).on('click', '.generate-variations', function () {
        // Logic to generate variations based on selected attributes
    });

    // Handle AJAX for saving attributes and variations
    $(document).on('click', '.save-rule', function () {
        // Logic to save the rule with attributes and variations via AJAX
    });
});
